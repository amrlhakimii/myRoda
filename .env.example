VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
# Only needed if your Firestore database was created under a custom name
# instead of the default one (Firebase console → Firestore Database → the
# name shown next to the database selector). Leave blank to use "(default)".
VITE_FIREBASE_DATABASE_ID=
